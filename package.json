{
  "name": "cleanup-repo",
  "version": "1.0.0",
  "description": "Simple tools for clean repo",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "G@urav M@ll",
  "license": "ISC",
  "hooks": {
    "pre-commit": "eslint && prettier"
  },
  "lint-staged": {
    "*.{js,ts}": "eslint --fix",
    "*.{css,scss}": "stylelint --fix",
    "*.{html,json}": "prettier --write"
  },
  "devDependencies": {
    "eslint": "^9.6.0",
    "husky": "^9.0.11",
    "lint-staged": "^15.2.7",
    "prettier": "^3.3.2",
    "stylelint": "^16.6.1",
    "stylelint-config-sass-guidelines": "^11.1.0",
    "stylelint-config-standard": "^36.0.1",
    "stylelint-prettier": "^5.0.0"
  }
}